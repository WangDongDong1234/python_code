1.三条边求最长的便l=max(a,max(b,c))
2.两只蚂蚁相遇后可以认为是保持原样继续通行
3.查找数组是否有某个元素，将数组排序，然后二分查找
4.a+b+c+d=m--->a+b=m-c-d  先将a+b的所有和枚举出来在用双成for循环计算m-c-d是否在和里面
5.求最短路径推荐用bfs
6.区间调度问题（比如尽可能参加最多的工作，贪心算法有一些情况是不能正确处理的  解决问题策略每次选最早结束的）
